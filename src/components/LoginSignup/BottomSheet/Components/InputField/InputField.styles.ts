import { StyleSheet } from "react-native";

export default StyleSheet.create({
  label: {
    fontSize: 16,
  },
  input: {
    alignItems: "center",
    borderWidth: 1,
    borderRadius: 16,
    borderColor: "#cdcdcd",
    flexDirection: "row",
    fontSize: 16,
    fontWeight: "400",
    color: "#000000ff",
  },
});
